import { BarTopSkeleton, ProductsSkeleton } from '@/app/ui/skeletons';

export default function Loading() {
    return <>
            < BarTopSkeleton />
            < ProductsSkeleton />
          </>
  }